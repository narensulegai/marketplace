export const recentBuyersTableStyles = theme => ({
  root: {
    width: "100%"
  },
  head: {
    background: "#0c68aa",
    color: theme.palette.text.white,
    fontWeight: "bold"
  },
  row: {
    "&:hover": {
      cursor: "pointer"
    }
  }
});